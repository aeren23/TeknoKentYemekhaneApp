@using Microsoft.AspNetCore.Components.Authorization
@using YemekhaneApp.Frontend.Models.Auth
@using YemekhaneApp.Frontend.Services
@inherits LayoutComponentBase

@inject IJSRuntime JS
@inject HttpClient Http
@inject NavigationManager Navigation
@inject AuthService AuthService
@inject AuthenticationStateProvider AuthStateProvider

@code {
    private bool _initialized = false;

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        // if (firstRender && !_initialized)
        // {
        //     _initialized = true;

        //     // JWT token'ı localStorage'dan oku ve provider'a bildir
        //     var token = await JS.InvokeAsync<string>("localStorage.getItem", "jwtToken");
        //     ((CustomAuthStateProvider)AuthStateProvider).SetToken(token);

        //     // User-agent kontrolü (varsa)
        //     var userAgent = await JS.InvokeAsync<string>("getUserAgent");
        //     var result = await AuthService.IsTrustedUserAgent(userAgent);
        //     if (result != null)
        //     {
        //         await JS.InvokeVoidAsync("localStorage.setItem", "jwtToken", result.Token);
        //         ((CustomAuthStateProvider)AuthStateProvider).SetToken(result.Token);
        //         Navigation.NavigateTo("/", true);
        //     }
        //     else
        //     {
        //         Navigation.NavigateTo("/login", true);
        //     }
        // }
    }
}

<div class="page">
    <div class="sidebar">
        <NavMenu />
    </div>

    <main>
        <div class="top-row px-4">
@*             <a href="https://learn.microsoft.com/aspnet/core/" target="_blank">About</a>
 *@        </div>

        <article class="content px-4">
            @Body
        </article>
    </main>
</div>

<div id="blazor-error-ui">
    An unhandled error has occurred.
    <a href="" class="reload">Reload</a>
    <a class="dismiss">🗙</a>
</div>
